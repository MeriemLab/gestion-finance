#from django.db import models

#class Commande_ligne(models.Model):
#   commande = models.ForeignKey('commandes.Commande', on_delete=models.CASCADE, related_name='commande_lignes')
#    produits = models.ForeignKey('produits.Produit', on_delete=models.CASCADE, related_name='commande_lignes')
#   quantity = models.PositiveIntegerField(default=1)  

 #   class Meta:
#        unique_together = (('commande', 'produits'),)

